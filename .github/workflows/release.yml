name: build-on-tag

on:
  push:
    tags:
      - "*-*"

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Parse tag
        id: vars
        run: |
          TAG=${GITHUB_REF_NAME}
          VERSION=${TAG%-*}
          IMAGE=${TAG##*-}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "image=$IMAGE" >> $GITHUB_OUTPUT

      - name: Lowercase owner + image
        id: lower
        run: |
          echo "owner=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_OUTPUT
          echo "image=${{ steps.vars.outputs.image }}" | tr '[:upper:]' '[:lower:]' | sed 's/^image=/image=/' >> $GITHUB_OUTPUT

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: images/${{ steps.vars.outputs.image }}
          push: true
          tags: ghcr.io/${{ steps.lower.outputs.owner }}/${{ steps.lower.outputs.image }}:${{ steps.vars.outputs.version }}
